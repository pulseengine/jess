project:
  name: falconeer
  version: "0.1.0"
  schemas:
    - common
    - dev
    - research
    - stpa
    - score
    - safety-case
    - supply-chain

externals:
  gale:  { path: ../gale,  prefix: gale }
  loom:  { path: ../loom,  prefix: loom }
  rivet: { path: ../rivet, prefix: rivet }

sources:
  - path: artifacts
    format: generic-yaml

docs:
  - docs

results: results
