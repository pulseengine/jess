package root:component;

world root {
  import wasi:clocks/wall-clock@0.2.4;
  import wasi:random/random@0.2.4;
  import wasi:io/error@0.2.4;
  import wasi:io/streams@0.2.4;
  import wasi:cli/stdout@0.2.4;
  import wasi:cli/stderr@0.2.4;
  import wasi:cli/stdin@0.2.4;
  import wasi:cli/environment@0.2.4;
  import wasi:cli/exit@0.2.4;
  import wasi:filesystem/types@0.2.4;
  import wasi:filesystem/preopens@0.2.4;

  export wasm-signatures:wasmsign/signing@0.2.6;
}
package wasi:clocks@0.2.4 {
  interface wall-clock {
    record datetime {
      seconds: u64,
      nanoseconds: u32,
    }
  }
}


package wasi:random@0.2.4 {
  interface random {
    get-random-bytes: func(len: u64) -> list<u8>;

    get-random-u64: func() -> u64;
  }
}


package wasi:io@0.2.4 {
  interface error {
    resource error;
  }
  interface streams {
    use error.{error};

    resource output-stream {
      check-write: func() -> result<u64, stream-error>;
      write: func(contents: list<u8>) -> result<_, stream-error>;
      blocking-write-and-flush: func(contents: list<u8>) -> result<_, stream-error>;
      blocking-flush: func() -> result<_, stream-error>;
    }

    resource input-stream;

    variant stream-error {
      last-operation-failed(error),
      closed,
    }
  }
}


package wasi:cli@0.2.4 {
  interface stdout {
    use wasi:io/streams@0.2.4.{output-stream};

    get-stdout: func() -> output-stream;
  }
  interface stderr {
    use wasi:io/streams@0.2.4.{output-stream};

    get-stderr: func() -> output-stream;
  }
  interface stdin {
    use wasi:io/streams@0.2.4.{input-stream};

    get-stdin: func() -> input-stream;
  }
  interface environment {
    get-environment: func() -> list<tuple<string, string>>;
  }
  interface exit {
    exit: func(status: result);
  }
}


package wasi:filesystem@0.2.4 {
  interface types {
    use wasi:io/streams@0.2.4.{error, output-stream};
    use wasi:clocks/wall-clock@0.2.4.{datetime};

    resource descriptor {
      write-via-stream: func(offset: filesize) -> result<output-stream, error-code>;
      append-via-stream: func() -> result<output-stream, error-code>;
      get-type: func() -> result<descriptor-type, error-code>;
      stat: func() -> result<descriptor-stat, error-code>;
    }

    enum error-code {
      access,
      would-block,
      already,
      bad-descriptor,
      busy,
      deadlock,
      quota,
      exist,
      file-too-large,
      illegal-byte-sequence,
      in-progress,
      interrupted,
      invalid,
      io,
      is-directory,
      loop,
      too-many-links,
      message-size,
      name-too-long,
      no-device,
      no-entry,
      no-lock,
      insufficient-memory,
      insufficient-space,
      not-directory,
      not-empty,
      not-recoverable,
      unsupported,
      no-tty,
      no-such-device,
      overflow,
      not-permitted,
      pipe,
      read-only,
      invalid-seek,
      text-file-busy,
      cross-device,
    }

    type filesize = u64;

    enum descriptor-type {
      unknown,
      block-device,
      character-device,
      directory,
      fifo,
      symbolic-link,
      regular-file,
      socket,
    }

    type link-count = u64;

    record descriptor-stat {
      %type: descriptor-type,
      link-count: link-count,
      size: filesize,
      data-access-timestamp: option<datetime>,
      data-modification-timestamp: option<datetime>,
      status-change-timestamp: option<datetime>,
    }

    filesystem-error-code: func(err: borrow<error>) -> option<error-code>;
  }
  interface preopens {
    use types.{descriptor};

    get-directories: func() -> list<tuple<descriptor, string>>;
  }
}


package wasm-signatures:wasmsign@0.2.6 {
  interface signing {
    record key-pair {
      public-key: list<u8>,
      secret-key: list<u8>,
    }

    record sign-options {
      key-id: option<list<u8>>,
      detached: bool,
    }

    variant sign-result {
      embedded(list<u8>),
      detached(list<u8>),
    }

    keygen: func() -> result<key-pair, string>;

    sign: func(module-bytes: list<u8>, secret-key: list<u8>, public-key: option<list<u8>>, options: sign-options) -> result<sign-result, string>;

    verify: func(module-bytes: list<u8>, public-key: list<u8>, detached-sig: option<list<u8>>) -> result<bool, string>;

    parse-public-key: func(key-bytes: list<u8>) -> result<list<u8>, string>;

    parse-secret-key: func(key-bytes: list<u8>) -> result<list<u8>, string>;

    to-pem-public: func(key-bytes: list<u8>) -> result<string, string>;

    to-pem-secret: func(key-bytes: list<u8>) -> result<string, string>;
  }
}
