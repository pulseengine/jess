{
  "_comment": "Baseline scry sound-analysis verdict for the meld-fused falcon core (REQ-PIX-018 leg b / TEST-PIX-022). Evidence snapshot, NOT the CI assertion: the gate asserts the SOUNDNESS INVARIANTS below (call_edges_unsound_fallback==0 && diag_unsound_fallback==0 && analysis completes), which are stable across falcon AND meld versions. The gate keys on invariants (version- and path-independent), NOT the fused sha. NOTE (meld#325, 2026-07-11): the scry gate now fuses `--reproducible` (removes the UUID/timestamp nondeterminism); the fused sha is byte-stable for a FIXED input path but still PATH-DEPENDENT (residual filed upstream), so it is deliberately NOT recorded here as a portable key. TEST-PIX-026 characterizes the reproducibility directly. Regenerate with: tools/scry-gate/run.sh > /tmp/v.json",
  "falcon_pin": "falcon-v1.112.0",
  "scry_sai_core": "1.18.0",
  "invariants_schema": "https://pulseengine.eu/scry-invariants/v1",
  "program_points": 771,
  "functions": 175,
  "reachable_from_exports": 95,
  "call_edges": 71,
  "call_edges_indirect": 12,
  "call_edges_unsound_fallback": 0,
  "taint_findings": 0,
  "diagnostics_total": 0,
  "diag_info": 0,
  "diag_warning": 0,
  "diag_unsound_fallback": 0
}
